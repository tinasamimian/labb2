public interface Movable {

    /**
     * In the interface we implement three methods.
     * The move method is intended to make the vehicle move.
     * The turnLeft method makes the car turn left and
     * the turnRight method gives the car a right direction
     */

    void move();
    void turnLeft();
    void turnRight();
}
